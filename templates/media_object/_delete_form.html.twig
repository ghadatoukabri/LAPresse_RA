<form method="post" action="{{ path('media_object_delete', {'id': media_object.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ media_object.id) }}">
    <button class="btn">Delete</button>
</form>
